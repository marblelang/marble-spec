use System.Console

fun Main() {
    for (i in 1..100) {
        if (i % 3 != 0 && i % 5 != 0) {
            WriteLine(i)
            continue
        }
        if (i % 3 == 0) Write("Fizz")
        if (i % 5 == 0) Write("Buzz")
        WriteLine()
    }
}